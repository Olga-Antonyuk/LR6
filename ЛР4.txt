#include <iostream>
#include <windows.h>

using namespace std;
new......
int main()
{
 SetConsoleCP(1251);
 

SetConsoleOutputCP(1251);
float a;
cout << "Input A:";
cin >> a;

(a > 0 || a == 0) ? cout << "Число А є позитивним або нулем" : cout << "Число А не є позитивним або нулем";
}
